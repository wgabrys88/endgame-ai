{
  "type": "json_schema",
  "json_schema": {
    "name": "reflector_output",
    "strict": true,
    "schema": {
      "type": "object",
      "properties": {
        "diagnosis": {
          "type": "string",
          "minLength": 30,
          "description": "Why stuck: what pattern is stuck and why — cite failures and stagnation"
        },
        "suggestion": {
          "type": "string",
          "minLength": 30,
          "description": "Concrete next approach: simpler step, different file, or ask for help"
        },
        "rule": {
          "type": "string",
          "description": "Short planner rule to append, or empty string if none"
        }
      },
      "required": ["diagnosis", "suggestion", "rule"],
      "additionalProperties": false
    }
  }
}