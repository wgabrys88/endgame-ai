{
  "type": "json_schema",
  "json_schema": {
    "name": "reflector_output",
    "strict": true,
    "schema": {
      "type": "object",
      "properties": {
        "diagnosis": {
          "type": "string",
          "minLength": 30,
          "description": "Why stuck: plan, desktop selector, SyntaxError, or LLM reject. Cite TRIGGER stag/pid."
        },
        "suggestion": {
          "type": "string",
          "minLength": 30,
          "description": "Concrete next approach: simpler step, re-observe desktop, or different milestone"
        },
        "rule": {
          "type": "string",
          "description": "Short planner rule to append, or empty string if none"
        }
      },
      "required": ["diagnosis", "suggestion", "rule"],
      "additionalProperties": false
    }
  }
}