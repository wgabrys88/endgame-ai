{
  "type": "json_schema",
  "json_schema": {
    "name": "verifier_output",
    "strict": true,
    "schema": {
      "type": "object",
      "properties": {
        "verdict": {
          "type": "string",
          "enum": ["confirmed", "denied"],
          "description": "confirmed only with file/output evidence"
        },
        "evidence": {
          "type": "string",
          "minLength": 20,
          "description": "What LAST_RESULT or filesystem proves"
        }
      },
      "required": ["verdict", "evidence"],
      "additionalProperties": false
    }
  }
}