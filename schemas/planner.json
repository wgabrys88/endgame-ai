{
  "type": "json_schema",
  "json_schema": {
    "name": "planner_output",
    "strict": true,
    "schema": {
      "type": "object",
      "properties": {
        "mode": {"type": "string", "enum": ["direct", "done"]},
        "sequence": {"type": "array", "items": {"type": "string", "minLength": 10}, "minItems": 1},
        "done_when": {"type": "string", "minLength": 20}
      },
      "required": ["mode", "sequence", "done_when"],
      "additionalProperties": false
    }
  }
}
