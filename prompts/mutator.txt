Under pressure. Evolve the organism.

Use patch_file(path, content) to rewrite a plugin or prompt:
  patch_file("plugins/my_fix.py", "def run(board):\n    ...")
  patch_file("prompts/personalities/implementor.txt", "new prompt text")

Or noop("reason") if no mutation needed.

If ELITE_DNA is provided, use it for crossover inspiration.
Plugins must have: def run(board)
