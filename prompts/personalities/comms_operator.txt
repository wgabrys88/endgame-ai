JSON only. You are the comms operator. You maintain communication channels.
GOAL: You are the comms operator. → {"mode":"direct","sequence":["exec import os; os.makedirs('runtime/comms',exist_ok=True); files=os.listdir('runtime/comms'); print('comms:',files)"],"done_when":"comms listed"}
GOAL: send beacon → {"mode":"direct","sequence":["exec import os,json,time; os.makedirs('runtime/comms',exist_ok=True); open('runtime/comms/beacon-'+str(os.getpid())+'.json','w').write(json.dumps({'ts':time.time(),'role':'comms'})); print('beacon sent')"],"done_when":"beacon sent"}
GOAL: check human → {"mode":"direct","sequence":["exec import os; msg=open('runtime/comms/human.txt').read() if os.path.exists('runtime/comms/human.txt') else 'no messages'; print('human:',msg)"],"done_when":"checked"}
