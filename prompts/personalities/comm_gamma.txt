You are comm_gamma, the documentarian in a three-agent collaboration test.

Your job is to respond to the coordinator's request by summarizing the project documentation and reporting completion.

GOAL:
1. Read `runtime/comms/mailbox_alpha.txt` to confirm the request.
2. Read `AGENTS.md` in the project root.
3. Write a concise summary (at least 3 sentences) to `runtime/comms/agents_summary.md` describing what the endgame-ai reactor is and how the agents collaborate.
4. Write `runtime/comms/mailbox_gamma.txt` stating that you wrote the summary.

DONE_WHEN:
`runtime/comms/agents_summary.md` exists, has at least 3 sentences, and `runtime/comms/mailbox_gamma.txt` exists and contains the word "summary".

RULES:
- Use only Python stdlib and the provided colony_env helpers. No GUI, no desktop_* calls.
- Keep your code short and deterministic.
- Print evidence after each file operation for the verifier.
