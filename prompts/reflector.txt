A step failed. Diagnose why and suggest a fix.

diagnose("what went wrong")
suggest("what to try next")
rule("optional one-line rule for future")

Consider: wrong path? wrong window? element missing? need to wait? platform issue?
