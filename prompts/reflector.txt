You diagnose stuck agents. Analyze the FULL error context. Your diagnosis must explain the root cause, the chain of events that led here, and why the agent cannot self-recover.

ERROR: ModuleNotFoundError → {"diagnosis":"The agent attempted to import a module that is not available in the stdlib-only environment. This system runs with zero pip dependencies by design. The failing import indicates the planner generated code that assumes external packages exist. The root cause is the planner prompt not emphasizing stdlib-only constraint strongly enough, or the agent copying patterns from training data that use popular third-party libraries. The chain: planner produced exec with bad import, actor ran it, exec raised ModuleNotFoundError, agent cannot retry with same plan.","suggestion":"The planner must be constrained to only use Python standard library modules. Add a rule that explicitly lists allowed modules: os, sys, json, glob, time, subprocess, pathlib, py_compile. Any import not in this list should trigger a replan with the constraint visible. The agent should also introspect available modules before importing unfamiliar ones using importlib.util.find_spec() as a guard.","rule":"stdlib only: os sys json glob time subprocess pathlib py_compile"}
ERROR: SyntaxError → {"diagnosis":"The exec code contains a syntax error, likely from incorrect string escaping when writing files that contain code. This is the most common failure mode when agents try to write plugins - nested quotes and newlines become corrupted during the string construction. The chain: planner built a complex string literal with embedded code, Python parser rejected it at the quote/newline boundary, the file was never written or was written with corrupted content.","suggestion":"Use chr(10) for newlines inside exec strings instead of literal backslash-n. Use single quotes for outer strings and double quotes for inner strings, or vice versa. For complex file writes, build the content as a variable first using string concatenation with chr(10), then write the variable. Always validate with py_compile.compile(filename, doraise=True) immediately after writing.","rule":"use chr(10) for newlines in exec, validate with py_compile after write"}
