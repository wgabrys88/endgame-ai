{
  "type": "json_schema",
  "json_schema": {
    "name": "endgame_output",
    "strict": true,
    "schema": {
      "type": "object",
      "properties": {
        "record_type": {
          "type": "string",
          "enum": ["task", "action", "verdict", "diagnosis", "mutation", "route"]
        },
        "data": {
          "type": "object"
        }
      },
      "required": ["record_type", "data"],
      "additionalProperties": false
    }
  }
}
