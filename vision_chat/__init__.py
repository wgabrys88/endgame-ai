"""Vision capture helpers (vendored from endgame-vision-chat).

Kept as a subpackage so the Loader (which seats only root-level *.py) does not
auto-seat these as tool nodes. The one seated node is vision.py at the repo root.
"""
