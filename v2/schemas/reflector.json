{
  "type": "json_schema",
  "json_schema": {
    "name": "self_reflection",
    "strict": true,
    "schema": {
      "type": "object",
      "properties": {
        "outcome": {
          "type": "string",
          "enum": ["success", "failure"],
          "description": "Overall assessment of recent trajectory."
        },
        "diagnosis": {
          "type": "string",
          "minLength": 10,
          "maxLength": 700,
          "description": "Root cause of current problems or stagnation, referencing concrete history."
        },
        "lesson_1": {
          "type": "string",
          "minLength": 10,
          "maxLength": 400,
          "description": "Primary actionable lesson from this reflection."
        },
        "lesson_2": {
          "type": "string",
          "minLength": 0,
          "maxLength": 400,
          "description": "Secondary lesson or empty."
        },
        "lesson_3": {
          "type": "string",
          "minLength": 0,
          "maxLength": 400,
          "description": "Tertiary lesson or empty."
        },
        "actor_prompt_rewrite": {
          "type": "string",
          "minLength": 0,
          "maxLength": 3000,
          "description": "Full replacement text for prompts/actor.txt if a meaningful improvement was found, else empty string."
        },
        "planner_prompt_rewrite": {
          "type": "string",
          "minLength": 0,
          "maxLength": 2000,
          "description": "Full replacement for prompts/planner.txt if a meaningful improvement was found, else empty string."
        },
        "verifier_prompt_rewrite": {
          "type": "string",
          "minLength": 0,
          "maxLength": 1500,
          "description": "Full replacement for prompts/verifier.txt if a meaningful improvement was found, else empty string."
        },
        "goal_rewrite": {
          "type": "string",
          "minLength": 0,
          "maxLength": 1200,
          "description": "A rephrased goal that preserves core intent while reducing confusion, or empty if current goal is fine."
        }
      },
      "required": ["outcome", "diagnosis", "lesson_1", "lesson_2", "lesson_3", "actor_prompt_rewrite", "planner_prompt_rewrite", "verifier_prompt_rewrite", "goal_rewrite"],
      "additionalProperties": false
    }
  }
}
